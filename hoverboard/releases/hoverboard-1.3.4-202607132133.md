# HoverBoard 1.3.4

## Fixed

- **Save to Session is visible again in Freeze.** The action was hidden inside the overflow menu, which Freeze mode does not show.
- **Save to Session is now a top-level toolbar button** in Draw, Freeze, and Whiteboard — icon only, no buried menu item.
